---
name: workflow
description: Implement a FitVibe feature end to end (requirements, code, tests, docs). Use when the user types /workflow or asks to execute the feature development workflow.
disable-model-invocation: true
---

# Feature workflow

Do the work in this conversation. Scope from `PROJECT_EPICS_AND_ACTIVITIES.md`, implement backend and frontend when both are needed, tests in `tests/`, docs only where behavior changed. Do not commit unless asked.
