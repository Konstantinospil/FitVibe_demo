name: CI

# Concurrency control: cancel in-progress runs for the same branch/PR
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: ["main", "dev", "stage"]
  pull_request:

# Default permissions (can be overridden per job for least privilege)
permissions:
  contents: read
  packages: write
  security-events: write
  id-token: write
  actions: read

env:
  REGISTRY: ghcr.io
  NODE_VERSION: "24"
  # Disable all caching
  TURBO_TOKEN: ""
  TURBO_REMOTE_CACHE: "false"
  PNPM_STORE_DIR: "/tmp/pnpm-store"

jobs:
  quality:
    name: Quality Gates (Lint & Type Check)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0 # Full history for better caching and blame

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Verify dependency integrity
        run: |
          pnpm audit --prod --audit-level=moderate || true
          echo "Dependency integrity check completed"

      - name: Lint
        timeout-minutes: 10
        run: pnpm lint:check

      - name: Type check
        timeout-minutes: 10
        run: pnpm typecheck

      - name: Job summary
        if: always()
        run: |
          echo "## Quality Gates Summary" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Lint: Passed" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Type Check: Passed" >> $GITHUB_STEP_SUMMARY

  backend_tests:
    name: Backend Tests
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      actions: read
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: fitvibe_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
      clamav:
        image: clamav/clamav:latest
        ports:
          - 3310:3310
        env:
          FRESHCLAM_CHECKS: 0
        options: >-
          --health-cmd="clamdscan --version"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/fitvibe_test
      # Individual PG env vars for test files to detect database
      PGHOST: 127.0.0.1
      PGPORT: 5432
      PGUSER: postgres
      PGPASSWORD: postgres
      PGDATABASE: fitvibe_test
      # Highest priority - test files check this first
      TEST_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/fitvibe_test
      CLAMAV_ENABLED: "true"
      CLAMAV_HOST: "127.0.0.1"
      CLAMAV_PORT: "3310"
      REAL_CLAMD: "1"
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable corepack
        run: corepack enable

      - name: Install native canvas prerequisites
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            build-essential \
            libcairo2-dev \
            libpango1.0-dev \
            libjpeg-dev \
            libgif-dev \
            librsvg2-dev \
            postgresql-client

      - name: Wait for services
        timeout-minutes: 15
        run: |
          echo "Waiting for PostgreSQL..."
          for i in {1..30}; do
            if pg_isready -h 127.0.0.1 -p 5432 -U postgres >/dev/null 2>&1; then
              echo "PostgreSQL is ready"
              break
            fi
            if [ $i -eq 30 ]; then
              echo "PostgreSQL failed to start" >&2
              exit 1
            fi
            sleep 2
          done

          echo "Waiting for ClamAV..."
          python3 - <<'PY'
          import socket, time, sys
          host = "127.0.0.1"
          port = 3310
          for i in range(30):
              try:
                  with socket.create_connection((host, port), timeout=2):
                      print("ClamAV is ready")
                      sys.exit(0)
              except OSError:
                  if i == 29:
                      print("ClamAV failed to start", file=sys.stderr)
                      sys.exit(1)
                  time.sleep(2)
          PY

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Backend unit tests & coverage
        timeout-minutes: 15
        run: pnpm --filter @fitvibe/backend exec jest --passWithNoTests --coverage --maxWorkers=2 --testPathIgnorePatterns="migrations/migrations\\.test\\.ts|seeds/seeds\\.test\\.ts|.*\\.integration\\.test\\.ts$"

      - name: Upload backend coverage artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: backend-coverage-${{ github.run_id }}
          path: apps/backend/coverage
          retention-days: 30
          if-no-files-found: error

      - name: Job summary
        if: always()
        run: |
          echo "## Backend Tests Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ Backend unit tests passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ Backend unit tests failed" >> $GITHUB_STEP_SUMMARY
          fi

  frontend_tests:
    name: Frontend Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      actions: read
    env:
      CI: "true"
      VITEST_MAX_THREADS: "2"
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable corepack
        run: corepack enable

      - name: Install native canvas prerequisites
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            build-essential \
            libcairo2-dev \
            libpango1.0-dev \
            libjpeg-dev \
            libgif-dev \
            librsvg2-dev \
            postgresql-client

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Frontend unit tests & coverage
        timeout-minutes: 10
        run: pnpm --filter @fitvibe/frontend test -- --coverage --maxWorkers=2

      - name: Verify frontend coverage output
        if: always()
        run: |
          echo "Frontend coverage directory:"
          ls -la apps/frontend/coverage || true
          if [ ! -f apps/frontend/coverage/coverage-summary.json ] && [ ! -f apps/frontend/coverage/coverage-final.json ]; then
            echo "::error::Frontend coverage reports were not written to apps/frontend/coverage"
            find apps/frontend -name 'coverage-summary.json' -o -name 'coverage-final.json' -o -name 'lcov.info' | head -20
            exit 1
          fi

      - name: Upload frontend coverage artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: frontend-coverage-${{ github.run_id }}
          path: apps/frontend/coverage
          retention-days: 30
          if-no-files-found: error

      - name: Job summary
        if: always()
        run: |
          echo "## Frontend Tests Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ Frontend unit tests passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ Frontend unit tests failed" >> $GITHUB_STEP_SUMMARY
          fi

  database_tests:
    name: Database Tests
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      actions: read
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: fitvibe_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    env:
      DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/fitvibe_test
      # Individual PG env vars for test files to detect database
      PGHOST: 127.0.0.1
      PGPORT: 5432
      PGUSER: postgres
      PGPASSWORD: postgres
      PGDATABASE: fitvibe_test
      # Highest priority - test files check this first
      TEST_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/fitvibe_test
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable corepack
        run: corepack enable

      - name: Install native canvas prerequisites
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            build-essential \
            libcairo2-dev \
            libpango1.0-dev \
            libjpeg-dev \
            libgif-dev \
            librsvg2-dev \
            postgresql-client \
            postgresql-client

      - name: Wait for PostgreSQL
        timeout-minutes: 10
        run: |
          echo "Waiting for PostgreSQL..."
          for i in {1..30}; do
            if pg_isready -h 127.0.0.1 -p 5432 -U postgres >/dev/null 2>&1; then
              echo "PostgreSQL is ready"
              echo "✓ Database is automatically provided by GitHub Actions service container"
              echo "  Connection: postgresql://postgres:postgres@127.0.0.1:5432/fitvibe_test"
              break
            fi
            if [ $i -eq 30 ]; then
              echo "PostgreSQL failed to start" >&2
              exit 1
            fi
            sleep 2
          done

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Run database migration tests
        timeout-minutes: 10
        run: |
          pnpm --filter @fitvibe/backend exec jest migrations/migrations.test.ts --maxWorkers=1 --forceExit

      - name: Run database seed tests
        timeout-minutes: 10
        run: |
          pnpm --filter @fitvibe/backend exec jest seeds/seeds.test.ts --maxWorkers=1 --forceExit

      - name: Force close database connections
        if: always()
        timeout-minutes: 2
        run: |
          echo "Closing all database connections..."
          PGPASSWORD=postgres psql -h 127.0.0.1 -p 5432 -U postgres -d fitvibe_test -c "SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname = 'fitvibe_test' AND pid <> pg_backend_pid();" || true
          echo "Database connections closed"

      - name: Job summary
        if: always()
        run: |
          echo "## Database Tests Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ Migration tests passed" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Seed tests passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ Database tests failed" >> $GITHUB_STEP_SUMMARY
          fi

  coverage:
    name: Coverage Gate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs:
      - backend_tests
      - frontend_tests
      - database_tests
    permissions:
      contents: read
      actions: read
    env:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Download backend coverage artifact
        uses: actions/download-artifact@v8
        with:
          name: backend-coverage-${{ github.run_id }}
          path: apps/backend/coverage

      - name: Download frontend coverage artifact
        uses: actions/download-artifact@v8
        with:
          name: frontend-coverage-${{ github.run_id }}
          path: apps/frontend/coverage

      - name: Aggregate coverage files
        timeout-minutes: 5
        run: |
          mkdir -p coverage
          find apps packages -name "lcov.info" -exec cat {} \; > coverage/lcov.info || true
          if [ ! -s coverage/lcov.info ]; then
            echo "No coverage files found" > coverage/lcov.info
          fi

      - name: Enforce coverage gate
        timeout-minutes: 10
        run: pnpm test:coverage:gate

      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-${{ github.run_id }}
          path: coverage
          retention-days: 30
          if-no-files-found: error

      - name: Upload coverage to Codecov
        if: success() && env.CODECOV_TOKEN != ''
        uses: codecov/codecov-action@v7
        with:
          files: ./coverage/lcov.info
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
          flags: unit
          name: codecov-umbrella

      - name: Job summary
        if: always()
        run: |
          echo "## Coverage Gate Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ Coverage gate passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ Coverage gate failed" >> $GITHUB_STEP_SUMMARY
          fi

  lighthouse:
    name: Lighthouse CI
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs:
      - quality
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install native canvas prerequisites
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            build-essential \
            libcairo2-dev \
            libpango1.0-dev \
            libjpeg-dev \
            libgif-dev \
            librsvg2-dev \
            postgresql-client

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Build frontend (SSR)
        run: pnpm --filter @fitvibe/frontend run build:ssr

      - name: Start SSR server
        timeout-minutes: 10
        env:
          NODE_ENV: production
          PORT: 4173
        run: |
          cd apps/frontend
          pnpm start:ssr &
          echo $! > ../ssr-server.pid
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4173/login >/dev/null 2>&1; then
              echo "SSR server started"
              exit 0
            fi
            sleep 2
          done
          echo "SSR server did not start in time" >&2
          exit 1

      - name: Run Lighthouse CI
        timeout-minutes: 10
        env:
          LHCI_PORT: "4173"
        # Login-only CI audit. Performance floor is 0.6 because React hydration TBT
        # dominates the category under simulated mobile; LCP/JS/a11y stay strict.
        run: pnpm exec lhci autorun --config=tests/perf/lighthouserc.ci.json --exit-on-error

      - name: Upload Lighthouse artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: lighthouse-report-${{ github.run_id }}
          path: tests/perf/lhci-output
          retention-days: 30
          if-no-files-found: error

      - name: Stop SSR server
        if: always()
        timeout-minutes: 1
        run: |
          pkill -f "tsx server.ts" || pkill -f "start:ssr" || true
          if [ -f apps/frontend/ssr-server.pid ]; then
            kill "$(cat apps/frontend/ssr-server.pid)" 2>/dev/null || true
            rm -f apps/frontend/ssr-server.pid
          fi

      - name: Job summary
        if: always()
        run: |
          echo "## Lighthouse Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ Lighthouse CI passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ Lighthouse CI failed" >> $GITHUB_STEP_SUMMARY
          fi

  i18n:
    name: i18n Coverage Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs:
      - quality
    permissions:
      contents: read
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Check i18n coverage
        timeout-minutes: 10
        run: pnpm i18n:check

      - name: Job summary
        if: always()
        run: |
          echo "## i18n Coverage Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ i18n coverage check passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ i18n coverage check failed" >> $GITHUB_STEP_SUMMARY
          fi

  contract_tests:
    name: API Contract Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs:
      - quality
      - backend_tests
    permissions:
      contents: read
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Generate OpenAPI spec
        timeout-minutes: 5
        run: pnpm -w openapi:build

      - name: Run OpenAPI contract tests
        timeout-minutes: 10
        env:
          CI: "true"
        run: pnpm --filter @fitvibe/backend exec jest tests/backend/contract/openapi.contract.test.ts --maxWorkers=1

      - name: Job summary
        if: always()
        run: |
          echo "## API Contract Tests Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ OpenAPI contract tests passed" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Zod ↔ OpenAPI schema alignment verified" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ API contract tests failed" >> $GITHUB_STEP_SUMMARY
            echo "- ⚠️ Schema drift detected between Zod and OpenAPI" >> $GITHUB_STEP_SUMMARY
          fi

  excluded_tests:
    name: Excluded Tests (Integration & Scripts)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs:
      - backend_tests
      - frontend_tests
      - database_tests
    permissions:
      contents: read
      actions: read
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: fitvibe_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
      clamav:
        image: clamav/clamav:latest
        ports:
          - 3310:3310
        env:
          FRESHCLAM_CHECKS: 0
        options: >-
          --health-cmd="clamdscan --version"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/fitvibe_test
      # Individual PG env vars for test files to detect database
      PGHOST: 127.0.0.1
      PGPORT: 5432
      PGUSER: postgres
      PGPASSWORD: postgres
      PGDATABASE: fitvibe_test
      # Highest priority - test files check this first
      TEST_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/fitvibe_test
      CLAMAV_ENABLED: "true"
      CLAMAV_HOST: "127.0.0.1"
      CLAMAV_PORT: "3310"
      REAL_CLAMD: "1"
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable corepack
        run: corepack enable

      - name: Install native canvas prerequisites
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            build-essential \
            libcairo2-dev \
            libpango1.0-dev \
            libjpeg-dev \
            libgif-dev \
            librsvg2-dev \
            postgresql-client \
            postgresql-client

      - name: Wait for services
        timeout-minutes: 15
        run: |
          echo "Waiting for PostgreSQL..."
          for i in {1..30}; do
            if pg_isready -h 127.0.0.1 -p 5432 -U postgres >/dev/null 2>&1; then
              echo "PostgreSQL is ready"
              break
            fi
            if [ $i -eq 30 ]; then
              echo "PostgreSQL failed to start" >&2
              exit 1
            fi
            sleep 2
          done

          echo "Waiting for ClamAV..."
          python3 - <<'PY'
          import socket, time, sys
          host = "127.0.0.1"
          port = 3310
          for i in range(30):
              try:
                  with socket.create_connection((host, port), timeout=2):
                      print("ClamAV is ready")
                      sys.exit(0)
              except OSError:
                  if i == 29:
                      print("ClamAV failed to start", file=sys.stderr)
                      sys.exit(1)
                  time.sleep(2)
          PY

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Run database migrations
        timeout-minutes: 10
        run: pnpm --filter @fitvibe/backend exec tsx src/db/utils/migrateAll.ts

      - name: Run scripts.test.ts
        timeout-minutes: 10
        env:
          CI: "true"
        run: pnpm --filter @fitvibe/backend exec jest tests/backend/db/utils/scripts.test.ts --maxWorkers=1 --forceExit

      - name: Run integration tests
        timeout-minutes: 20
        env:
          NODE_ENV: test
          CI: "true"
        # maxWorkers=1: these suites truncate a shared Postgres DB and IP-keyed
        # brute-force counters collide when two workers run in parallel.
        run: pnpm --filter @fitvibe/backend exec jest --maxWorkers=1 --testMatch="**/integration/**/*.integration.test.ts" --testPathIgnorePatterns="/node_modules/|verification-resend-limit\\.test\\.ts$|login-enumeration\\.test\\.ts$" --forceExit

      - name: Job summary
        if: always()
        run: |
          echo "## Excluded Tests Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ Scripts test passed" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Integration tests passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ Some excluded tests failed" >> $GITHUB_STEP_SUMMARY
          fi

  openapi_spec:
    name: Build OpenAPI spec
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs:
      - quality
      - database_tests
    permissions:
      contents: read
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable corepack
        run: corepack enable

      - name: Install native canvas prerequisites
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            build-essential \
            libcairo2-dev \
            libpango1.0-dev \
            libjpeg-dev \
            libgif-dev \
            librsvg2-dev \
            postgresql-client

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Generate OpenAPI JSON
        timeout-minutes: 5
        run: pnpm -w openapi:build

      - name: Validate OpenAPI schema
        timeout-minutes: 2
        run: |
          if [ ! -f apps/backend/openapi/openapi.json ]; then
            echo "Error: OpenAPI file not generated" >&2
            exit 1
          fi
          node -e "JSON.parse(require('fs').readFileSync('apps/backend/openapi/openapi.json', 'utf8'))"

      - name: Upload OpenAPI artifact
        if: success()
        uses: actions/upload-artifact@v7
        with:
          name: openapi-${{ github.run_id }}
          path: apps/backend/openapi/openapi.json
          retention-days: 90
          if-no-files-found: error

  integration:
    name: Backend Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs:
      - backend_tests
      - frontend_tests
      - database_tests
      - excluded_tests
    permissions:
      contents: read
      actions: read
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: fitvibe
          POSTGRES_USER: fitvibe
          POSTGRES_PASSWORD: fitvibe
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U fitvibe -d fitvibe"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      clamav:
        image: clamav/clamav:latest
        ports:
          - 3310:3310
        env:
          FRESHCLAM_CHECKS: 0
        options: >-
          --health-cmd="clamdscan --version"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://fitvibe:fitvibe@127.0.0.1:5432/fitvibe
      CLAMAV_ENABLED: "true"
      CLAMAV_HOST: "127.0.0.1"
      CLAMAV_PORT: "3310"
      REAL_CLAMD: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install native canvas prerequisites
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            build-essential \
            libcairo2-dev \
            libpango1.0-dev \
            libjpeg-dev \
            libgif-dev \
            librsvg2-dev \
            postgresql-client

      - name: Wait for services
        timeout-minutes: 10
        run: |
          echo "Waiting for PostgreSQL..."
          for i in {1..30}; do
            if pg_isready -h 127.0.0.1 -p 5432 -U fitvibe >/dev/null 2>&1; then
              echo "PostgreSQL is ready"

              break
            fi
            if [ $i -eq 30 ]; then
              echo "PostgreSQL failed to start" >&2
              exit 1
            fi
            sleep 2
          done

          echo "Waiting for ClamAV..."
          python3 - <<'PY'
          import socket, time, sys
          host = "127.0.0.1"
          port = 3310
          for i in range(30):
              try:
                  with socket.create_connection((host, port), timeout=2):
                      print("ClamAV is ready")
                      sys.exit(0)
              except OSError:
                  if i == 29:
                      print("ClamAV failed to start", file=sys.stderr)
                      sys.exit(1)
                  time.sleep(2)
          PY

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Run database migrations
        timeout-minutes: 10
        run: pnpm --filter @fitvibe/backend exec tsx src/db/utils/migrateAll.ts

      - name: Verify migrations completed successfully
        timeout-minutes: 2
        run: |
          echo "Verifying critical tables exist..."
          PGPASSWORD=fitvibe psql -h 127.0.0.1 -p 5432 -U fitvibe -d fitvibe <<EOF
          DO \$\$
          DECLARE
            missing_tables text[];
          BEGIN
            SELECT array_agg(t) INTO missing_tables
            FROM unnest(ARRAY['users', 'profiles', 'sessions', 'feed_items', 'exercises']) t
            WHERE NOT EXISTS (
              SELECT 1 FROM information_schema.tables
              WHERE table_name = t
            );

            IF array_length(missing_tables, 1) > 0 THEN
              RAISE EXCEPTION 'Missing critical tables: %', array_to_string(missing_tables, ', ');
            END IF;
          END \$\$;
          EOF

      - name: Seed database
        timeout-minutes: 10
        env:
          NODE_ENV: test
        run: pnpm --filter @fitvibe/backend exec tsx src/db/utils/seedAll.ts

      - name: Integration tests
        timeout-minutes: 20
        env:
          NODE_ENV: test
        # Shared test DB cannot be truncated safely by parallel Jest workers.
        run: pnpm --filter @fitvibe/backend exec jest --maxWorkers=1 --testMatch="**/integration/**/*.integration.test.ts" --testPathIgnorePatterns="/node_modules/|verification-resend-limit\\.test\\.ts$|login-enumeration\\.test\\.ts$"

      - name: Job summary
        if: always()
        run: |
          echo "## Integration Tests Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ Integration tests passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ Integration tests failed" >> $GITHUB_STEP_SUMMARY
          fi

  metrics_contract:
    name: Metrics Contract
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs:
      - quality
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Validate metrics exposure
        timeout-minutes: 3
        run: node tests/metrics/assert-prom.cjs

  security:
    name: Security Scans
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs:
      - quality
    permissions:
      contents: read
      security-events: write
      actions: read
    env:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0 # Full history for secret scanning

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install native canvas prerequisites
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            build-essential \
            libcairo2-dev \
            libpango1.0-dev \
            libjpeg-dev \
            libgif-dev \
            librsvg2-dev \
            postgresql-client

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Dependency audit (fail on high severity)
        timeout-minutes: 5
        run: pnpm audit --prod --audit-level=high

      - name: Scan for CVEs (OSV)
        timeout-minutes: 10
        uses: google/osv-scanner-action/osv-scanner-action@v2.2.4
        with:
          scan-args: |-
            --lockfile=pnpm-lock.yaml
            --format=json
            --output=osv-report.json

      - name: Upload OSV report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: osv-report-${{ github.run_id }}
          path: osv-report.json
          retention-days: 90
          if-no-files-found: ignore

      - name: Snyk scan
        if: env.SNYK_TOKEN != ''
        timeout-minutes: 10
        uses: snyk/actions/node@master
        with:
          command: test
          args: --all-projects --severity-threshold=high
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        continue-on-error: true

      - name: TruffleHog OSS scan
        timeout-minutes: 5
        uses: trufflesecurity/trufflehog@v3.90.13
        with:
          path: "./"
          base: ${{ github.event.repository.default_branch || 'main' }}
          extra_args: "--results=verified"

      - name: Static secret scan
        timeout-minutes: 5
        run: node tests/security/secret-scan.cjs

      - name: Container vulnerability scan
        timeout-minutes: 10
        uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1 # v0.35.0; SHA-pinned after GHSA-69fq-xp46-6x23
        with:
          scan-type: fs
          scan-ref: .
          format: table
          severity: HIGH,CRITICAL
          exit-code: 1
          ignore-unfixed: true
          skip-dirs: |
            infra/keys
            infra/docker/keys
            node_modules

      - name: Job summary
        if: always()
        run: |
          echo "## Security Scan Summary" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Dependency audit completed" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ OSV scan completed" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Secret scan completed" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Container scan completed" >> $GITHUB_STEP_SUMMARY

  codeql:
    name: CodeQL Static Analysis
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs:
      - quality
    permissions:
      contents: read
      security-events: write
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: javascript
          queries: security-and-quality
          config-file: ./.github/codeql/codeql-config.yml

      - name: Autobuild
        uses: github/codeql-action/autobuild@v4

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:javascript"

      - name: Job summary
        if: always()
        run: |
          echo "## CodeQL Analysis Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ CodeQL static analysis completed" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ CodeQL analysis failed" >> $GITHUB_STEP_SUMMARY
          fi

  zap_baseline:
    name: OWASP ZAP Baseline
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: security
    permissions:
      contents: read
      security-events: write
    env:
      ZAP_TARGET_URL: ${{ secrets.ZAP_TARGET_URL }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run ZAP baseline scan
        if: env.ZAP_TARGET_URL != ''
        timeout-minutes: 12
        uses: zaproxy/action-baseline@v0.12.0
        with:
          target: ${{ env.ZAP_TARGET_URL }}
          rules_file_name: .zap/rules.tsv
          cmd_options: -a -I

  performance:
    name: Performance Budgets
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs:
      - quality
      - lighthouse
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install native canvas prerequisites
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            build-essential \
            libcairo2-dev \
            libpango1.0-dev \
            libjpeg-dev \
            libgif-dev \
            librsvg2-dev \
            postgresql-client

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Install k6
        uses: grafana/setup-k6-action@v1

      - name: Build frontend
        run: pnpm --filter @fitvibe/frontend run build

      - name: Start mock services
        timeout-minutes: 10
        run: |
          node tests/perf/mock-server.mjs &
          echo $! > mock-server.pid
          for i in {1..20}; do
            if curl -fsS http://127.0.0.1:4173/health >/dev/null; then
              echo "Mock server started"
              exit 0
            fi
            sleep 3
          done
          echo "Mock server did not start in time" >&2
          exit 1

      - name: Run k6 smoke test
        timeout-minutes: 10
        env:
          API_BASE_URL: http://127.0.0.1:4173/health
        run: k6 run --summary-export=tests/perf/k6-summary.json tests/perf/k6-smoke.js

      - name: Assert k6 performance budgets
        timeout-minutes: 10
        run: node tests/perf/assert-budgets.cjs tests/perf/k6-summary.json

      - name: Stop mock server
        if: always()
        timeout-minutes: 1
        run: |
          pkill -f "mock-server.mjs" || true
          if [ -f mock-server.pid ]; then
            kill "$(cat mock-server.pid)" 2>/dev/null || true
            rm -f mock-server.pid
          fi
          sleep 1

      - name: Upload performance artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: perf-metrics-${{ github.run_id }}
          path: |
            tests/perf/k6-summary.json
          retention-days: 30
          if-no-files-found: error

      - name: Job summary
        if: always()
        run: |
          echo "## Performance Budgets Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ k6 performance tests passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ Performance tests failed" >> $GITHUB_STEP_SUMMARY
          fi

  qa_summary:
    name: QA Summary
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs:
      - quality
      - coverage
      - lighthouse
      - i18n
      - performance
      - contract_tests
      - visual_regression
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Download coverage artifact
        uses: actions/download-artifact@v8
        with:
          name: coverage-${{ github.run_id }}
          path: coverage

      - name: Download performance metrics
        uses: actions/download-artifact@v8
        with:
          name: perf-metrics-${{ github.run_id }}
          path: .

      - name: Download lighthouse report
        uses: actions/download-artifact@v8
        with:
          name: lighthouse-report-${{ github.run_id }}
          path: tests/perf/lhci-output

      - name: Generate QA summary
        timeout-minutes: 5
        run: node tests/qa/generate-summary.mjs

      - name: Upload QA summary artifact
        uses: actions/upload-artifact@v7
        with:
          name: qa-summary-${{ github.run_id }}
          path: tests/qa/summary
          retention-days: 90
          if-no-files-found: error

  accessibility:
    name: Accessibility (axe)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs:
      - quality
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install native canvas prerequisites
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            build-essential \
            libcairo2-dev \
            libpango1.0-dev \
            libjpeg-dev \
            libgif-dev \
            librsvg2-dev \
            postgresql-client

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Build frontend bundle
        timeout-minutes: 10
        run: pnpm --filter @fitvibe/frontend run build

      - name: Install Playwright browsers
        timeout-minutes: 10
        run: pnpm exec playwright install --with-deps

      - name: Run axe accessibility suite
        timeout-minutes: 10
        env:
          PLAYWRIGHT_JOBS: 1
        run: pnpm test:a11y

      - name: Upload accessibility artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-a11y-report-${{ github.run_id }}
          path: |
            playwright-report
            test-results
          retention-days: 30
          if-no-files-found: ignore

  visual_regression:
    name: Visual Regression Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs:
      - quality
      - frontend_tests
    permissions:
      contents: read
    # Same Playwright Linux image used by `pnpm test:visual:linux` so font
    # rasterization matches the committed *-linux.png baselines.
    container:
      image: mcr.microsoft.com/playwright:v1.57.0-jammy
      options: --ipc=host
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Build frontend
        timeout-minutes: 10
        run: pnpm --filter @fitvibe/frontend run build:visual

      - name: Start preview server
        timeout-minutes: 10
        working-directory: apps/frontend
        run: |
          if [ ! -f dist/client/index.html ]; then
            echo "Missing dist/client/index.html after build:visual" >&2
            ls -la dist dist/client 2>/dev/null || true
            exit 1
          fi
          # `pnpm --filter … exec` keeps the workspace root as cwd, so Vite would
          # preview an empty repo-root dist/client (HTTP 404) while still printing Local.
          pnpm exec vite preview --host 0.0.0.0 --port 4173 --strictPort --outDir dist/client &
          echo $! > "${GITHUB_WORKSPACE}/preview-server.pid"
          python3 - <<'PY'
          import http.client
          import sys
          import time

          last = "no attempt"
          for _ in range(60):
              try:
                  conn = http.client.HTTPConnection("127.0.0.1", 4173, timeout=2)
                  conn.request("GET", "/")
                  response = conn.getresponse()
                  status = response.status
                  response.read()
                  conn.close()
                  if 200 <= status < 400:
                      print(f"Preview server started (HTTP {status})")
                      sys.exit(0)
                  last = f"HTTP {status}"
              except Exception as exc:
                  last = str(exc)
              time.sleep(2)
          print(f"Preview server did not start in time ({last})", file=sys.stderr)
          sys.exit(1)
          PY

      - name: Run visual regression tests
        timeout-minutes: 20
        env:
          APP_URL: "http://127.0.0.1:4173"
          CI: "true"
          DISABLE_WEBSERVER: "true"
        run: pnpm exec playwright test --config tests/frontend/visual/config/playwright.config.ts

      - name: Stop preview server
        if: always()
        run: |
          if [ -f preview-server.pid ]; then
            kill $(cat preview-server.pid) 2>/dev/null || true
            rm -f preview-server.pid
          fi

      - name: Upload visual test artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: visual-regression-${{ github.run_id }}
          path: |
            tests/frontend/visual/__screenshots__
            playwright-report/visual
            test-results/visual-junit.xml
          retention-days: 30
          if-no-files-found: ignore

      - name: Job summary
        if: always()
        run: |
          echo "## Visual Regression Tests Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ Visual regression tests passed" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ All screenshots match baseline (maxDiffPixelRatio ≤ 0.2%)" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ Visual regression tests failed" >> $GITHUB_STEP_SUMMARY
            echo "- ⚠️ Visual differences detected - review screenshots" >> $GITHUB_STEP_SUMMARY
          fi

  e2e:
    name: E2E Playwright
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: integration
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/stage')
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install native canvas prerequisites
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            build-essential \
            libcairo2-dev \
            libpango1.0-dev \
            libjpeg-dev \
            libgif-dev \
            librsvg2-dev \
            postgresql-client

      - name: Install project dependencies
        run: pnpm install --frozen-lockfile --no-offline

      - name: Install Playwright browsers
        timeout-minutes: 10
        run: pnpm exec playwright install --with-deps

      - name: Build frontend
        timeout-minutes: 10
        run: pnpm --filter @fitvibe/frontend exec pnpm run build

      - name: Run Playwright smoke tests
        timeout-minutes: 15
        run: pnpm exec playwright test --config tests/frontend/e2e/playwright.config.cjs --reporter=line

      - name: Upload Playwright artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report-${{ github.run_id }}
          path: |
            playwright-report
            test-results
          retention-days: 30
          if-no-files-found: ignore

  # Native amd64/arm64 builds. QEMU emulation of `pnpm fetch` on GitHub's
  # x86_64 runners routinely exceeds the image-build timeout.
  build-platform:
    name: Build images (${{ matrix.platform }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 35
    needs:
      - quality
      - backend_tests
      - frontend_tests
      - database_tests
      - coverage
      - excluded_tests
      - integration
      - metrics_contract
      - accessibility
      - qa_summary
      - performance
      - security
      - codeql
      - openapi_spec
      - lighthouse
      - i18n
      - contract_tests
      - visual_regression
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/stage')
    permissions:
      contents: read
      packages: write
      id-token: write
      actions: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            runner: ubuntu-24.04
          - platform: linux/arm64
            runner: ubuntu-24.04-arm
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set lowercase GHCR image names
        run: |
          owner="$(printf '%s' "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')"
          platform_pair="${{ matrix.platform }}"
          platform_pair="${platform_pair//\//-}"
          echo "OWNER=${owner}" >> "$GITHUB_ENV"
          echo "BACKEND_IMAGE=ghcr.io/${owner}/fitvibe-backend" >> "$GITHUB_ENV"
          echo "FRONTEND_IMAGE=ghcr.io/${owner}/fitvibe-frontend" >> "$GITHUB_ENV"
          echo "PLATFORM_PAIR=${platform_pair}" >> "$GITHUB_ENV"

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend image by digest
        id: backend
        timeout-minutes: 25
        uses: docker/build-push-action@v7
        with:
          context: .
          file: infra/docker/prod/Dockerfile.backend
          platforms: ${{ matrix.platform }}
          provenance: true
          sbom: true
          tags: ${{ env.BACKEND_IMAGE }}
          outputs: type=image,push-by-digest=true,name-canonical=true,push=true
          cache-from: type=gha,scope=backend-${{ env.PLATFORM_PAIR }}
          cache-to: type=gha,mode=max,scope=backend-${{ env.PLATFORM_PAIR }}

      - name: Build and push frontend image by digest
        id: frontend
        timeout-minutes: 25
        uses: docker/build-push-action@v7
        with:
          context: .
          file: infra/docker/prod/Dockerfile.frontend
          platforms: ${{ matrix.platform }}
          provenance: true
          sbom: true
          tags: ${{ env.FRONTEND_IMAGE }}
          outputs: type=image,push-by-digest=true,name-canonical=true,push=true
          cache-from: type=gha,scope=frontend-${{ env.PLATFORM_PAIR }}
          cache-to: type=gha,mode=max,scope=frontend-${{ env.PLATFORM_PAIR }}

      - name: Export digests
        run: |
          mkdir -p "${RUNNER_TEMP}/backend-digests" "${RUNNER_TEMP}/frontend-digests"
          backend_digest="${{ steps.backend.outputs.digest }}"
          frontend_digest="${{ steps.frontend.outputs.digest }}"
          touch "${RUNNER_TEMP}/backend-digests/${backend_digest#sha256:}"
          touch "${RUNNER_TEMP}/frontend-digests/${frontend_digest#sha256:}"

      - name: Upload backend digest
        uses: actions/upload-artifact@v7
        with:
          name: backend-digests-${{ env.PLATFORM_PAIR }}
          path: ${{ runner.temp }}/backend-digests/*
          if-no-files-found: error
          retention-days: 1

      - name: Upload frontend digest
        uses: actions/upload-artifact@v7
        with:
          name: frontend-digests-${{ env.PLATFORM_PAIR }}
          path: ${{ runner.temp }}/frontend-digests/*
          if-no-files-found: error
          retention-days: 1

  build:
    name: Build & Publish Images
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs:
      - build-platform
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/stage')
    permissions:
      contents: read
      packages: write
      id-token: write
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set lowercase GHCR image names
        run: |
          owner="$(printf '%s' "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')"
          echo "OWNER=${owner}" >> "$GITHUB_ENV"
          echo "BACKEND_IMAGE=ghcr.io/${owner}/fitvibe-backend" >> "$GITHUB_ENV"
          echo "FRONTEND_IMAGE=ghcr.io/${owner}/fitvibe-frontend" >> "$GITHUB_ENV"

      - name: Determine package versions
        id: versions
        timeout-minutes: 2
        run: |
          if [ ! -f apps/backend/package.json ] || [ ! -f apps/frontend/package.json ]; then
            echo "Error: package.json files not found" >&2
            exit 1
          fi
          backend=$(node -p "require('./apps/backend/package.json').version")
          frontend=$(node -p "require('./apps/frontend/package.json').version")
          echo "backend=${backend}" >> "$GITHUB_OUTPUT"
          echo "frontend=${frontend}" >> "$GITHUB_OUTPUT"

      - name: Download backend digests
        uses: actions/download-artifact@v8
        with:
          path: ${{ runner.temp }}/backend-digests
          pattern: backend-digests-*
          merge-multiple: true

      - name: Download frontend digests
        uses: actions/download-artifact@v8
        with:
          path: ${{ runner.temp }}/frontend-digests
          pattern: frontend-digests-*
          merge-multiple: true

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create multi-arch manifests
        id: manifests
        timeout-minutes: 5
        run: |
          set -euo pipefail

          backend_count=$(find "${RUNNER_TEMP}/backend-digests" -type f | wc -l)
          frontend_count=$(find "${RUNNER_TEMP}/frontend-digests" -type f | wc -l)
          if [ "${backend_count}" -lt 2 ] || [ "${frontend_count}" -lt 2 ]; then
            echo "Expected 2 platform digests per image (amd64 + arm64)." >&2
            echo "backend=${backend_count} frontend=${frontend_count}" >&2
            exit 1
          fi

          backend_refs=""
          for digest_file in "${RUNNER_TEMP}/backend-digests"/*; do
            backend_refs="${backend_refs} ${BACKEND_IMAGE}@sha256:$(basename "${digest_file}")"
          done

          frontend_refs=""
          for digest_file in "${RUNNER_TEMP}/frontend-digests"/*; do
            frontend_refs="${frontend_refs} ${FRONTEND_IMAGE}@sha256:$(basename "${digest_file}")"
          done

          docker buildx imagetools create \
            -t "${BACKEND_IMAGE}:sha-${GITHUB_SHA}" \
            -t "${BACKEND_IMAGE}:v${{ steps.versions.outputs.backend }}" \
            ${backend_refs}

          docker buildx imagetools create \
            -t "${FRONTEND_IMAGE}:sha-${GITHUB_SHA}" \
            -t "${FRONTEND_IMAGE}:v${{ steps.versions.outputs.frontend }}" \
            ${frontend_refs}

          backend_digest=$(docker buildx imagetools inspect "${BACKEND_IMAGE}:sha-${GITHUB_SHA}" | awk '/^Digest:/{print $2; exit}')
          frontend_digest=$(docker buildx imagetools inspect "${FRONTEND_IMAGE}:sha-${GITHUB_SHA}" | awk '/^Digest:/{print $2; exit}')

          if [ -z "${backend_digest}" ] || [ -z "${frontend_digest}" ]; then
            echo "Failed to resolve merged image digests" >&2
            exit 1
          fi

          echo "backend_digest=${backend_digest}" >> "$GITHUB_OUTPUT"
          echo "frontend_digest=${frontend_digest}" >> "$GITHUB_OUTPUT"

      - name: Install cosign
        uses: sigstore/cosign-installer@v3

      - name: Sign backend image
        timeout-minutes: 5
        env:
          COSIGN_EXPERIMENTAL: "1"
        run: cosign sign --yes --keyless ${{ env.BACKEND_IMAGE }}@${{ steps.manifests.outputs.backend_digest }}

      - name: Sign frontend image
        timeout-minutes: 5
        env:
          COSIGN_EXPERIMENTAL: "1"
        run: cosign sign --yes --keyless ${{ env.FRONTEND_IMAGE }}@${{ steps.manifests.outputs.frontend_digest }}

      - name: Generate backend SBOM
        timeout-minutes: 5
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.BACKEND_IMAGE }}@${{ steps.manifests.outputs.backend_digest }}
          output-file: sbom-backend.spdx.json

      - name: Generate frontend SBOM
        timeout-minutes: 5
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.FRONTEND_IMAGE }}@${{ steps.manifests.outputs.frontend_digest }}
          output-file: sbom-frontend.spdx.json

      - name: Record image digests
        timeout-minutes: 2
        run: |
          printf "backend=%s@%s\nfrontend=%s@%s\n" \
            "${{ env.BACKEND_IMAGE }}" "${{ steps.manifests.outputs.backend_digest }}" \
            "${{ env.FRONTEND_IMAGE }}" "${{ steps.manifests.outputs.frontend_digest }}" > image-digests.txt

      - name: Download OpenAPI artifact
        uses: actions/download-artifact@v8
        with:
          name: openapi-${{ github.run_id }}
          path: artifacts/openapi

      - name: Upload release artifacts
        uses: actions/upload-artifact@v7
        with:
          name: container-artifacts-${{ github.run_id }}
          path: |
            sbom-backend.spdx.json
            sbom-frontend.spdx.json
            image-digests.txt
            artifacts/openapi/openapi.json
          retention-days: 365
          if-no-files-found: error

      - name: Create Release
        if: github.ref == 'refs/heads/main'
        id: create_release
        timeout-minutes: 5
        uses: ncipollo/release-action@v1
        with:
          tag: v${{ steps.versions.outputs.backend }}
          name: Release v${{ steps.versions.outputs.backend }}
          body: |
            - Backend image: ${{ env.BACKEND_IMAGE }}:v${{ steps.versions.outputs.backend }}
            - Frontend image: ${{ env.FRONTEND_IMAGE }}:v${{ steps.versions.outputs.frontend }}
          draft: false
          prerelease: false
          artifacts: container-artifacts-${{ github.run_id }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Job summary
        if: always()
        run: |
          echo "## Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Backend image: \`${{ env.BACKEND_IMAGE }}@${{ steps.manifests.outputs.backend_digest }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Frontend image: \`${{ env.FRONTEND_IMAGE }}@${{ steps.manifests.outputs.frontend_digest }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Images signed with cosign" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ SBOMs generated" >> $GITHUB_STEP_SUMMARY

  build_verification:
    name: Build Verification (Container Smoke Tests)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs:
      - build
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/stage')
    permissions:
      contents: read
      packages: read
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: fitvibe_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    env:
      DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/fitvibe_test
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set lowercase GHCR image names
        run: |
          owner="$(printf '%s' "${GITHUB_REPOSITORY_OWNER}" | tr '[:upper:]' '[:lower:]')"
          echo "OWNER=${owner}" >> "$GITHUB_ENV"
          echo "BACKEND_IMAGE=ghcr.io/${owner}/fitvibe-backend" >> "$GITHUB_ENV"
          echo "FRONTEND_IMAGE=ghcr.io/${owner}/fitvibe-frontend" >> "$GITHUB_ENV"

      - name: Install postgresql-client
        timeout-minutes: 2
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y postgresql-client

      - name: Wait for PostgreSQL
        timeout-minutes: 5
        run: |
          echo "Waiting for PostgreSQL..."
          for i in {1..30}; do
            if pg_isready -h 127.0.0.1 -p 5432 -U postgres >/dev/null 2>&1; then
              echo "PostgreSQL is ready"
              break
            fi
            if [ $i -eq 30 ]; then
              echo "PostgreSQL failed to start" >&2
              exit 1
            fi
            sleep 2
          done

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull backend image
        timeout-minutes: 5
        run: docker pull ${{ env.BACKEND_IMAGE }}:sha-${{ github.sha }}

      - name: Verify backend image size
        timeout-minutes: 2
        run: |
          BACKEND_TAG="${{ env.BACKEND_IMAGE }}:sha-${{ github.sha }}"
          SIZE=$(docker images "$BACKEND_TAG" --format "{{.Size}}" | head -1)
          echo "Backend image size: $SIZE"
          # Extract numeric size (e.g., "1.2GB" -> 1.2)
          NUM_SIZE=$(echo "$SIZE" | sed 's/[^0-9.]//g' | head -c 4)
          if [ -z "$NUM_SIZE" ] || [ "$NUM_SIZE" = "0" ]; then
            echo "Warning: Could not parse image size"
          else
            echo "Backend image size: ${NUM_SIZE} (parsed)"
          fi

      - name: Start backend container
        timeout-minutes: 5
        run: |
          docker run -d \
            --name test-backend \
            -p 4100:4100 \
            -e DATABASE_URL="${{ env.DATABASE_URL }}" \
            -e NODE_ENV=production \
            -e PORT=4100 \
            ${{ env.BACKEND_IMAGE }}:sha-${{ github.sha }}
          echo "Backend container started"

      - name: Wait for backend health check
        timeout-minutes: 30
        run: |
          echo "Waiting for backend health endpoint..."
          for i in {1..60}; do
            if curl -f http://127.0.0.1:4100/health >/dev/null 2>&1; then
              echo "Backend health check passed"
              exit 0
            fi
            if [ $i -eq 60 ]; then
              echo "Backend health check failed after 60 attempts" >&2
              docker logs test-backend
              exit 1
            fi
            sleep 2
          done

      - name: Verify backend health endpoint
        timeout-minutes: 2
        run: |
          RESPONSE=$(curl -s http://127.0.0.1:4100/health)
          echo "Health endpoint response: $RESPONSE"
          if echo "$RESPONSE" | grep -q "ok\|healthy\|status"; then
            echo "✅ Backend health endpoint working"
          else
            echo "❌ Backend health endpoint returned unexpected response"
            exit 1
          fi

      - name: Pull frontend image
        timeout-minutes: 5
        run: docker pull ${{ env.FRONTEND_IMAGE }}:sha-${{ github.sha }}

      - name: Verify frontend image size
        timeout-minutes: 2
        run: |
          FRONTEND_TAG="${{ env.FRONTEND_IMAGE }}:sha-${{ github.sha }}"
          SIZE=$(docker images "$FRONTEND_TAG" --format "{{.Size}}" | head -1)
          echo "Frontend image size: $SIZE"
          NUM_SIZE=$(echo "$SIZE" | sed 's/[^0-9.]//g' | head -c 4)
          if [ -z "$NUM_SIZE" ] || [ "$NUM_SIZE" = "0" ]; then
            echo "Warning: Could not parse image size"
          else
            echo "Frontend image size: ${NUM_SIZE} (parsed)"
          fi

      - name: Start frontend container
        timeout-minutes: 5
        run: |
          docker run -d \
            --name test-frontend \
            -p 8080:4173 \
            -e NODE_ENV=production \
            -e PORT=4173 \
            ${{ env.FRONTEND_IMAGE }}:sha-${{ github.sha }}
          echo "Frontend container started"

      - name: Wait for frontend SSR server
        timeout-minutes: 10
        run: |
          echo "Waiting for frontend SSR server..."
          for i in {1..30}; do
            if curl -f http://127.0.0.1:8080/health >/dev/null 2>&1; then
              echo "Frontend SSR server is ready"
              exit 0
            fi
            if [ $i -eq 30 ]; then
              echo "Frontend SSR server failed to start after 30 attempts" >&2
              docker logs test-frontend
              exit 1
            fi
            sleep 2
          done

      - name: Verify frontend health endpoint
        timeout-minutes: 2
        run: |
          RESPONSE=$(curl -s http://127.0.0.1:8080/health)
          echo "Health endpoint response: $RESPONSE"
          if echo "$RESPONSE" | grep -qi "ok\|status"; then
            echo "✅ Frontend health endpoint working"
          else
            echo "❌ Frontend health endpoint returned unexpected response"
            exit 1
          fi

      - name: Verify frontend serves SSR HTML
        timeout-minutes: 2
        run: |
          RESPONSE=$(curl -s http://127.0.0.1:8080/ | head -20)
          if echo "$RESPONSE" | grep -qi "<!doctype html\|<html"; then
            echo "✅ Frontend serves SSR HTML"
            # Verify SSR content is present
            if echo "$RESPONSE" | grep -qi "<div id=\"root\">"; then
              echo "✅ SSR content detected in HTML"
            else
              echo "⚠️ SSR content may not be present"
            fi
          else
            echo "❌ Frontend does not serve HTML"
            echo "Response: $RESPONSE"
            exit 1
          fi

      - name: Cleanup containers
        if: always()
        timeout-minutes: 2
        run: |
          docker stop test-backend test-frontend 2>/dev/null || true
          docker rm test-backend test-frontend 2>/dev/null || true

      - name: Job summary
        if: always()
        run: |
          echo "## Build Verification Summary" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ Backend container starts and health check passes" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Frontend container starts and serves HTML" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Image sizes verified" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ Build verification failed" >> $GITHUB_STEP_SUMMARY
            echo "- ⚠️ Containers may not be functioning correctly" >> $GITHUB_STEP_SUMMARY
          fi
