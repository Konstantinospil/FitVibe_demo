NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://fitvibe:fitvibe@db:5432/fitvibe

# Database connection (individual variables for Knex)
PGHOST=db
PGPORT=5432
PGDATABASE=fitvibe
PGUSER=fitvibe
PGPASSWORD=fitvibe

# JWT (paths relative to backend working dir if you use file-based keys)
JWT_PRIVATE_KEY_PATH=/app/keys/jwt_rs256.key
JWT_PUBLIC_KEY_PATH=/app/keys/jwt_rs256.pub

CORS_ORIGIN=http://localhost:5173
FRONTEND_URL=http://localhost:5173

# Email Configuration (SMTP)
# For development, you can use Gmail with an App Password
# See apps/docs/SMTP_Setup_Guide.md for detailed setup instructions
# Email Configuration (Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=fitvibe.test01@gmail.com
SMTP_PASS=qjzlacbuirqrqefc
SMTP_FROM_NAME=FitVibe
SMTP_FROM_EMAIL=fitvibe.test01@gmail.com

# Enable email features
EMAIL_ENABLED=true
